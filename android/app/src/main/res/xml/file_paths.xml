<?xml version="1.0" encoding="utf-8"?>
<!-- Declara qué carpetas puede exponer el FileProvider — path_provider's
     getTemporaryDirectory() en Android apunta a la caché de la app
     (context.cacheDir), de ahí "cache-path". -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="actualizaciones" path="." />
</paths>
