<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Requerido por local_auth para el inicio de sesión con huella/rostro. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Requerido por google_mobile_ads (y por el cliente HTTP) para acceder a internet. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Requerido por geolocator para autocompletar la dirección de entrega
         con la ubicación GPS del dispositivo. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Requerido por firebase_messaging (Android 13+) para poder mostrar
         notificaciones push. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Requerido por image_picker para fotografiar el QR real de Yape/Plin
         como alternativa a elegirlo de la galería. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <application
        android:label="Panadería Ronceros"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- ID real de la app "Panadería Ronceros" en Google AdMob. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8167336469205854~3402166298"/>
        <!-- Ícono/color con el que FCM dibuja las notificaciones push en la
             barra de estado — sin esto, Android usa el ícono de la app
             completo (se ve mal, sin transparencia) o un ícono genérico. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Requerido por url_launcher (Android 11+) para abrir el marcador
             telefónico, WhatsApp y Google Maps desde el módulo de Clientes. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Requerido (Android 11+) para poder detectar/abrir la app de
             Yape desde el botón "Ir a pagar" al cobrar una deuda. -->
        <package android:name="com.bcp.innovacxion.yapeapp"/>
        <!-- Requerido (Android 11+) para poder abrir Play Store directo con
             el esquema market:// desde ActualizacionRequeridaPage. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="market"/>
        </intent>
    </queries>
</manifest>
